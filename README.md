# QueryDecoder
**Extract tgWebAppData** is a straightforward web tool designed to extract the `tgWebAppData` parameter from a URL. This tool simplifies the process of retrieving and copying the `tgWebAppData` parameter, making it accessible and easy to use directly from your browser.
